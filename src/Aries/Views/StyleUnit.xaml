<UserControl x:Class="Aries.Views.StyleUnit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="水印样式" Margin="2" BorderBrush="{DynamicResource PrimaryBrush}">
            <hc:PropertyGrid SelectedObject="{Binding WaterMaskManager}" />
        </GroupBox>

        <GroupBox Grid.Row="1" Header="背景样式" Margin="2" BorderBrush="{DynamicResource PrimaryBrush}">
            <hc:PropertyGrid SelectedObject="{Binding BackGroundManager}" />
        </GroupBox>

    </Grid>
</UserControl>
