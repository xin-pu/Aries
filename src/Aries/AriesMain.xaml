<hc:Window x:Class="Aries.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="Aries" Height="768" Width="1366"
        Icon="Aries.ico"
        ShowTitle="True"
        ShowNonClientArea="True"
        WindowState="Maximized">
    <hc:Window.NonClientAreaContent>
        <TabControl DockPanel.Dock="Top" Height="100" Margin="5">
            <TabItem Header="文件" FontSize="16" Width="80"/>
            <TabItem Header="开始" FontSize="16" Width="80"/>
            <TabItem Header="帮助" FontSize="16" Width="80"/>
        </TabControl>
    </hc:Window.NonClientAreaContent>
    <DockPanel>

      
        
        <StatusBar DockPanel.Dock="Bottom" Height="40" BorderThickness="0,0,0,0">
            <TextBlock Style="{DynamicResource MyTextBlock}" Text="&#xef21;"/>
            
        </StatusBar>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <hc:SideMenu BorderThickness="1" Width="300" ExpandMode="ShowOne"
                         Grid.Column="0"
                         Margin="2">
                <hc:Interaction.Triggers>
                    <hc:EventTrigger EventName="SelectionChanged">
                        <hc:EventToCommand Command="{Binding SwitchItemCmd}" PassEventArgsToCommand="True" />
                    </hc:EventTrigger>
                </hc:Interaction.Triggers>
            </hc:SideMenu>
            <Canvas Grid.Column="1"/>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <hc:PropertyGrid Grid.Row="0"></hc:PropertyGrid>
                <GroupBox Grid.Row="1" Background="#9918FE"/>
            </Grid>
        </Grid>
        

    </DockPanel>
</hc:Window>
