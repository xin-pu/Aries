<UserControl x:Class="AriesCV.Views.CVPropertyGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:AriesCV.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="350"
             MinWidth="300"
             MaxWidth="450"
             DataContext="{Binding CvWorkerContainer.GraphCvAreaAtWorkSpace.SelectBlockVertex,
                            Source={StaticResource Locator}}">
    <controls:CvPropertyGrid x:Name="PropertyGrid" 
                              SelectedObject="{Binding}"
                              FontSize="11"
                              MaxWidth="400"/>
</UserControl>
